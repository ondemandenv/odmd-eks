{
  "name": "app",
  "version": "1.0.0",
  "main": "app.js",
  "dependencies": {
    "@aws-sdk/client-sts": "^3.515.0",
    "@aws-sdk/client-codecommit": "^3.515.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.134",
    "@types/node": "^20.11.19",
    "@types/js-yaml": "^4.0.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "@aws-sdk/types": "^3.515.0",
    "esbuild": "^0.20.1"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w",
    "ts": "ts-node index.ts",
    "debug": "tsc --build --clean && tsc --build && ts-node debug.ts"
  }
}
